{% extends 'base.html.twig' %}

{% block title %}Top Burgers{% endblock %}

{% block body %}
    <h1>Top {{ limit }} Burgers les plus chers</h1>

    {% if burgers is empty %}
        <p>Aucun burger trouvé.</p>
    {% else %}
        <ul>
            {% for burger in burgers %}
                <main class="container">
                    <div class="row">
                        <div class="col s12 m8 offset-m2">
                            <div class="card">
                                <div class="card-image">
                                    <img src="{{ burger.getImage() }}" alt="Image du {{ burger.name }}">
                                </div>
                                <div class="card-content">
                                    <h2 class="center-align">{{ burger.name }}</h2>
                                    <p>Numéro du burger : {{ burger.id }}</p>
                                    <p>{{ burger.description }}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </main>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}